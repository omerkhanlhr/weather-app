[project]
name = "weather-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "omerkhanlhr", email = "omar.championcr@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "python-dotenv>=1.2.3",
    "requests>=2.34.2",
]

[project.scripts]
weather-app = "weather_app:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
